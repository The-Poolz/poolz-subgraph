import { BigInt, Bytes } from "@graphprotocol/graph-ts"

export function handleMassBuildRefundCreation(poolId: BigInt, owner: Bytes, provider: Bytes): void {
    // This function is currently empty but can be implemented as needed
}
